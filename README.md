# Habit Tracker — Test Task

Проект содержит три части:

- `backend` — FastAPI сервер, SQLite, REST API
- `bot` — Telegram bot на `aiogram`, работает с backend через HTTP
- `frontend` — простая React (TypeScript) SPA (скелет)

Инструкции по запуску описаны в соответствующих папках.
